{
  "name": "iconfont",
  "version": "1.0.0",
  "description": "Browser-based icon font viewer — load local TTF/WOFF fonts and browse icons like iconify.design",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "build:singlefiles": "node ./scripts/make-singlefiles.js",
    "build:all": "npm run build && npm run build:singlefiles",
    "preview": "vite preview"
  },
  "dependencies": {
    "opentype.js": "^1.3.4"
  },
  "devDependencies": {
    "@fluentui/svg-icons": "^1.1.331",
    "@types/opentype.js": "^1.3.10",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  }
}
